name: tests

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  bundled-test:
    strategy:
      matrix:
        os: [
#          ubuntu-latest,
          macos-latest,
#          macos-14, # macOS arm runner
#          windows-latest,
        ]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v3
      - name: Install requirements on ubuntu
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y libblas-dev liblapack-dev zlib1g-dev libmetis-dev
      - name: Install requirements on macos
        if: matrix.os == 'macos-latest' || matrix.os == 'macos-14'
        run: |
          brew install gcc
      - name: Test bundled
        run: |
          export DYLD_LIBRARY_PATH=/opt/homebrew/opt/gcc@14/lib/gcc/14:$DYLD_LIBRARY_PATH
          cargo b --features bundled 
          cargo t --features bundled create
          cargo t --features bundled --examples

#  from-source-test:
#    strategy:
#      matrix:
#        os: [
#          macos-latest,
#          macos-14,
#          ubuntu-latest,
#        ]
#    runs-on: ${{ matrix.os }}
#    steps:
#      - uses: actions/checkout@v3
#      - name: Install bison
#        if: ${{ matrix.os == 'macos-latest' || matrix.os == 'macos-14' }}
#        run: |
#          brew install bison
#      - name: Test from-source
#        run: |
#          cargo b --features from-source -vv
#          cargo t --features from-source create
#          cargo t --features from-source --examples

# TODO: fix this, needs tbb
#  windows-from-source:
#    runs-on: windows-latest
#    steps:
#      - uses: actions/checkout@v3
#
#      - name: Test from-source
#        run: |
#          echo "${{ runner.workspace }}/vcpkg" >> $GITHUB_PATH
#          cargo b --features from-source -vv
#          cargo t --features from-source create
#          cargo t --features from-source --examples